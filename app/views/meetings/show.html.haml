%p#notice= notice

.row
  .span7
    .page-header
      %h2
        = @meeting.name
        %small= I18n.l(@meeting.date)
    %p.lead
      = @meeting.description

    %em
      This meeting is invite only. If you would like to receive an invitation,
      =mail_to @city.email, "let us know why."

    %br
    %br


= render partial: "shared/coaches", locals: { coaches: @meeting.coaches , title: "Attending Coaches" }

- if Meeting.all_sponsors.any?
  %section
    %legend Sponsors

    .row
      - Meeting.all_sponsors.each do |sponsor|
        .span2
          =image_tag sponsor.image_url

