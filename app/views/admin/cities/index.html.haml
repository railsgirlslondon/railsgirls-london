%h2 Cities

%table.table
  %tr
    %th Name
    %th Email
    %th twitter

  - @cities.each do |city|
    %tr
      %td= link_to city.name, [:admin, city]
      %td= city.email
      %td= city.twitter

= link_to "Add city", new_admin_city_path
