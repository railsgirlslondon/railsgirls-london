%legend New city
= simple_form_for [:admin, City.new], html: { class: 'form-horizontal' } do |f|

  .form-inputs
    = f.input :name
    = f.input :twitter
    = f.input :email

  .form-actions
    = f.button :submit, class: 'btn btn-large'
