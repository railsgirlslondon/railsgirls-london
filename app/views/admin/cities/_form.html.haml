= simple_form_for [:admin, @city], html: { class: 'form-horizontal' } do |f|

  .form-inputs
    = f.input :name
    = f.input :email
    = f.input :twitter

  .form-actions
    = f.button :submit, class: 'btn btn-large'
